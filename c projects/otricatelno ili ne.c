# include <stdio.h>

int main (void)
{
int num;
printf("enter an integer: ");
scanf("%d",&num);
if (num<0) printf("Number is negative.");
else printf("number is not-negative.");

return 0 ;

}

