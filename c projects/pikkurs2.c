#inlude<stdio.h>

int main(){
	int number,simpleNumber=0,i=0,arr[i];
	
	printf("Enter number");
	scanf("%d",&number);
	
	for(i;i<number;i++){ 
		scanf("%d, arr[i]");
	}
	for(j=2;j<umber;j++){
		if (number == 1){
			
	    	simpleNumber += number	;
		}
		else{
			flag =0;
			
		for(number;i++)   
			}
			flag = 1;
			simpleNumber+= number;
			
			if (flag) simpleNumber += arr[i];
		}
	}
	printf("Sum is:  %d" simpleNumber)
	return 0;
}
